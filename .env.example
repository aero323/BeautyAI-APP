# Optional: public URL for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="http://localhost:3001"

# This project currently runs locally without a Gemini API key.
# If LLM-backed features are added later, keep provider keys server-side only.
