# BeautyAI 学员端产品笔记

## 当前项目判断

学员端目前是一个移动端原型，已经覆盖“首页任务入口、AI 知识问答、AI 陪练、剧本演练、每日句子跟读、在线考试、个人学习档案”的主要界面骨架。

与 `Beauty-AI-main` 后台相比，学员端已经承接了后台的部分业务闭环，但大多仍是静态 mock 数据和前端模拟交互，尚未接入真实数据、任务分发、课程内容、LLM、语音识别、考试提交或学习记录。

## 已实现的学员端能力

- 首页：学员身份、每日任务、AI 知识助手入口、AI Roleplay 入口、每日句子入口。
- AI 知识问答：具备聊天界面、分类筛选、输入发送、加载态和固定回答。
- AI 陪练：具备顾客角色列表、角色详情卡片、对话页、录音按钮、提示区、完成报告。
- 剧本演练：具备剧本入口、引导/挑战模式、步骤进度、录音模拟、剧本完成报告。
- 句子跟读：具备每日句子卡片、上下滑切换、录音练习次数、听读/跟读控件。
- 考试：具备考试列表、考试说明、AI 监考提示、计时答题页、切题、提交和结果复盘。
- 个人中心：具备学习时长、AI Roleplay 次数和排行榜预览。

## 已落地的近期迭代

- 登录：已提供两个 mock 学员入口，分别代表雅加达大区店员和泗水大区店员。
- 区域数据：登录后任务、课程、练习、考试、问答和个人中心会按所选大区读取 mock 数据。
- 首页任务化：已移除首页原有 `AI Roleplay` 和 `Sentence of Day` 功能陈列，改为后台发布任务驱动的任务收件箱。
- 任务闭环：学习任务、练习任务、考试任务统一出现在首页和任务列表页，支持状态、截止时间、进入任务和完成回传。
- 完成回传：课程页可手动标记完成；练习结果页和考试结果页会自动把对应任务标记为完成，并持久化到本地 `localStorage`。
- 多任务实例：任务已从“按类型合并”升级为“按任务实例展示”，支持同类型不同来源、不同周期、不同进度的任务同时存在。
- 示例：雅加达店员会同时收到总部下发的“新品集中培训每日练习 0/1”和雅加达大区下发的“区域限定话术每周练习 1/3”，首页和任务列表会分别展示来源、周期、内容范围、进度和截止时间。
- 完成计入：练习完成后会按任务规则推进匹配任务；当前 mock 中允许同一敏感肌练习同时计入总部每日任务和雅加达每周区域任务。

## 主要缺口

- 课程学习未实现：`Course` 页面仍是施工占位，未承接后台的在线课件、学习任务、课程完成状态。
- 数据未打通：所有任务、考试、角色、剧本、句子、成绩、排行榜都来自本地静态数据。
- AI 能力未真实接入：知识问答、陪练对话、剧本演练、评分报告都是 `setTimeout` 模拟，没有 LLM、ASR、TTS 或评分服务。
- 任务闭环不足：后台可创建学习任务、练习任务、考试任务，但学员端缺少统一任务列表、待办状态、截止时间、完成提交和回传。
- 考试能力仍是原型：题目固定、答案不保存、不计分、不提交，AI 监考只是文案和弹窗模拟。
- 学习记录不足：没有课程进度、练习历史、考试历史、错题本、成就、学习路径。
- 用户与组织上下文缺失：没有登录、门店/区域/角色绑定，也无法体现不同学员收到不同任务。

## MVP 优先级建议

1. 先打通后台发布到学员端的任务闭环：学习任务、练习任务、考试任务统一出现在首页/任务列表，支持状态、截止时间和完成回传。
2. 补齐课程学习：展示后台课件，支持学习进度、完成确认、课后题或关联考试。
3. 把考试从原型变成可提交流程：读取题库/试卷、保存作答、提交评分、展示错题解析。
4. 把 AI 陪练变成可评估练习：先接入真实剧本/角色数据，再逐步接入语音识别、LLM 对话和评分报告。
5. 建立个人学习档案：汇总学习、练习、考试、排名和薄弱点，供学员自查，也供后台看板使用。

## 待确认问题

- 学员端首期是否必须接真实 LLM，还是先完成任务/考试/课程数据闭环？
- 学员端是否需要登录体系，还是先用固定 demo 用户？
- 后台和学员端之间是否已有 API 规划，还是需要先定义本地 mock API/schema？
- AI 陪练首期重点是文字对话、语音对话，还是剧本评分？

## 任务模型讨论草案

当学员同时收到多个同类型任务时，不能按任务类型合并展示。任务应按“任务实例”展示和追踪，而不是只按“学习/练习/考试”分类。

建议任务模型拆为：

- 任务定义：后台创建的任务规则，例如全国新品每日练习、雅加达区域每周话术练习。
- 任务实例：某个学员在某个周期收到的具体待办，例如“本周雅加达话术练习 0/3”。
- 完成记录：学员每次学习、练习、考试的提交记录，可按规则计入一个或多个任务实例。

首页建议按时间和紧急度展示任务，例如“今日必做”“本周任务”“即将截止”，同类型任务可以分组但不能合并丢失来源。任务卡片需要展示来源、周期、进度和截止时间。

例子：

- 全国任务：新品集中培训，每日 1 次，来源=总部，周期=每日，今日进度=0/1。
- 区域任务：雅加达限定话术练习，每周 3 次，来源=雅加达大区，周期=本周，周进度=1/3。

如果一次练习是否可以同时计入多个任务，需要由后台任务规则决定。默认不同来源、不同内容的任务分别完成；只有内容范围一致且后台允许复用时，才可以一条完成记录同时计入多个任务。

## 多内容任务的 APP 端处理方案

后台创建学习任务时可能勾选多门课件，创建练习任务时可能组合多个数字人、场景剧本和金句/句子跟读。因此 APP 端进入任务后不应直接跳到某一个内容页，而应先进入“任务详情 / 播放列表”页。

学习任务：

- 按后台勾选顺序展示课件列表。
- 顶部展示任务来源、周期、截止时间、整体进度。
- 主按钮为“继续学习”，自动进入下一个未完成课件。
- 每个课件有独立状态：未开始、学习中、已完成。
- 任务完成规则默认是所有必修课件完成后任务完成。

练习任务：

- 进入后展示任务内的练习清单，而不是直接进入某个练习。
- 清单按素材类型分组：数字人顾客、场景剧本、金句/句子跟读。
- 顶部展示频次目标，例如今日 0/1、本周 1/3。
- 主按钮为“继续练习”，默认进入下一个推荐练习单元。
- 单元可以自由选择，但系统应推荐顺序：未完成必做项优先，其次低分项，其次未练过素材。
- 对“每周 3 次”这类频次任务，完成记录按练习次数累积；对“指定 3 个场景都要通关”这类覆盖任务，需要每个指定单元都有完成状态。

因此任务需要支持两类完成规则：

- 覆盖型：所有指定内容单元都完成，例如 3 门课件、2 个剧本、5 条句子。
- 频次型：在指定内容池中完成 N 次，例如每日练 1 次、每周练 3 次。

如果一个任务同时有多个内容单元和频次要求，APP 应展示两层进度：任务总进度和内容单元状态。例如“本周 1/3 次，已覆盖 1/2 个剧本”。

## Profile / 学习档案改造结论

后台 `Beauty-AI-main` 里看到 BA 学习数据的核心不是普通个人资料，而是“学习履约档案”。管理端在人员档案和 dashboard 中关注的是：任务完成率、最近考试成绩、总学习课件、累计陪练时长、近期考试记录、近期任务状态、练习/打卡表现、知识薄弱点和学习提示。

APP 当前 Profile 还停留在头像、门店、学习时长、AI Roleplay 次数和排行榜，和后台数据口径不一致，也容易显得像通用 demo 页。学员端应该改造成“我的学习档案”，让 BA 自己看到后台正在衡量的同一组数据，只是表达方式从“被管理”改成“自查与改进”。

建议 APP Profile 首屏结构：

- 身份区：姓名、门店、区域、岗位，保留退出登录入口。
- 核心指标：任务完成率、最新考试分数、已学课件数、累计陪练时长或本月练习次数。
- 学习与考试：展示最近 3 次考试记录，包含考试名称、日期、分数、是否达标。
- 练习与打卡：展示本月对练次数/是否达标、剧本练习进度、金句跟读连续天数或表现评价。
- 近期任务：复用首页任务模型，展示最近学习、练习、考试任务状态和进度。
- 学习提示：根据错题或练习表现给出薄弱知识点和下一步建议，例如“夏季新品系列”“敏感肌适用肤质”等。
- 排行榜：可以保留，但优先级下降，作为底部的区域排名摘要，而不是 Profile 的主体。

不建议继续把 `Total Study Hours` 和 `AI Roleplay` 作为最主要指标，除非后台明确也用这两个口径。更推荐直接对齐后台字段：`taskCompletionRate`、`lastExamScore`、`totalCoursesStudied`、`totalPracticeTime`、`recentExams`、`recentTasks`，再补充练习表现与学习提示。

数据实现上，短期可以在 `RegionDataset` 增加 `learningProfile` mock 对象；中期应从真实任务、课程、考试、练习记录派生，避免 Profile、首页任务和课程/练习/考试页各自维护一套不一致的数据。

### 大区排名算法建议

大区排名应使用简单、可见、容易解释的积分制，不做 100 分归一化，也不引入复杂时间系数。学员能从 APP 上看到自己为什么涨分，培训师也能用后台数据解释排名。

排名对象是在同一大区、同一统计周期内的 BA。默认使用月榜，后续可以增加周榜。榜单展示“学习积分”，按积分从高到低排序。

建议积分规则：

- 完成学习任务：每个任务 +20 分。
- 完成练习任务：每个任务 +20 分。
- 完成考试任务：每个任务 +20 分。
- 学完课件：每个课件 +5 分。
- 有效练习：每次 +5 分。有效练习包括 AI 陪练、剧本练习、金句跟读，但必须来自任务要求或练习页的正式完成记录。
- 考试成绩奖励：考试通过后按成绩给奖励分，`考试奖励分 = round(考试分数 / 10)`。例如 95 分加 10 分，82 分加 8 分。未通过不加奖励分，但如果考试任务规则允许“提交即完成”，仍可拿考试任务完成分。

推荐公式：

`学习积分 = 已完成任务数 * 20 + 已学课件数 * 5 + 有效练习次数 * 5 + 考试奖励分合计`

这个规则的好处是每一分都有可见来源：

- 首页/任务详情能看到任务是否完成。
- 课程页能看到课件是否学完。
- 练习页能看到练习是否完成。
- 考试页能看到考试分数。

为了避免刷分，只保留几个简单限制：

- 同一个课件只计一次学完积分。
- 同一个练习任务的有效练习次数最多按任务要求次数计分，例如任务要求每周 3 次，最多按 3 次加练习分。
- 同一场考试只取最高一次有效成绩奖励。
- 后台可以标记任务是否参与排名；临时测试任务、草稿任务不计入积分。

示例：

- 本月完成 4 个任务：4 * 20 = 80 分。
- 学完 6 个课件：6 * 5 = 30 分。
- 完成 7 次有效练习：7 * 5 = 35 分。
- 两场考试分别 95 分和 82 分：10 + 8 = 18 分。
- 本月学习积分 = 80 + 30 + 35 + 18 = 163 分。

排名按学习积分从高到低排序。同分时，建议依次按“完成任务数更多、考试奖励分更高、有效练习次数更多、已学课件数更多”排序。

### 积分结算周期

学习积分不应只在月底统一计算。学员端应采用“平时实时累计，月底冻结归档”的机制。

- 学员完成任务、课件、练习、考试后，积分即时更新。
- APP Profile 的大区排名展示“本月截至当前”的实时积分和实时排名。
- 每月最后一天 23:59 后冻结当月榜单，生成月度排名记录。
- 次月 1 日开启新的月榜，学习积分从 0 开始累计。
- 历史月榜需要保留，APP 可查看上月/历史月份，后台可查看并导出。

这样学员做完动作后能马上看到积分和排名变化，有激励感；月底结算只负责定榜、归档和用于管理复盘。

## Practice 模块与后台资产对齐结论

后台 `Beauty-AI-main` 的 AI 陪练资产分成三类：

- 数字人顾客：核心配置是数字人名称、静态头像、视频形象、角色标签、人格设定 prompt、对话流程/剧本大纲、适用范围。
- 场景剧本：核心配置是剧本名称、场景介绍、场景配图、分步骤脚本、每一步的提示词。
- 金句库：核心配置是产品分类/产品线/产品、产品介绍、产品图片、具体金句内容、金句使用提示、适用范围。

后台 `PracticeTaskManage` 进一步说明，练习任务并不是只有一种素材，而是可以组合多个数字人顾客、多个场景剧本、多个金句跟读资产一起下发。

APP 当前 Practice 模块的优点是已经做出了三种入口和任务详情页，且 `PracticeTaskDetail` 已能按“数字人顾客 / 场景剧本 / 金句跟读”拆分展示任务资产，这是对的。

但从资产对齐角度看，APP 现在做多了这些：

- 数字人顾客详情页过早做成了完整聊天产品，包含录音、提示开关、Finish、对话气泡、评分报告等一整套重交互，而后台资产本身只配置了角色设定和流程，不代表首期一定要做这么重的陪练体验。
- 场景剧本详情页硬编码了 6 步进度、固定对话推进和剧本完成报告，实际上后台配置的是“可变步骤数 + 每步提示”，APP 现在反而把剧本结构写死了。
- 金句练习被做成了“Sentence of Day”式的全屏滑动内容流，视觉和交互都更像内容消费产品，不像后台的“产品金句库”。
- 各类结果页给出了很多自定义评分维度和建议文案，例如 Product Knowledge、Fluency、Cross-selling、步骤得分等，这些在后台资产配置中并没有对应字段，容易显得前端先行编造了一套评估体系。

APP 现在少了这些真正该有的内容：

- 数字人顾客页缺少角色标签、适用范围、人格设定摘要、对话目标摘要。也就是说，学员进入练习前，缺少“这个顾客是谁、我为什么练她、重点考什么”的资产说明。
- 场景剧本页没有真正消费后台的“场景介绍、场景配图、步骤列表、每步提示”，导致剧本页虽然有聊天壳子，但没有把后台剧本资产本体展示出来。
- 金句练习页没有“产品 -> 金句”的对应关系。后台金句是挂在具体产品上的，并且有使用提示；APP 现在只剩句子本身和图片，丢掉了产品上下文。
- 三类练习资产都缺少总部/区域范围感知。后台资产存在 `scope`，但 APP 学员端几乎没有把这一点显式表达出来。
- 自由练入口页的分组方式还不是后台资产结构。数字人顾客更适合按标签/范围筛选，金句更适合按产品线或产品筛选，而不是泛化成统一的内容流。

如果进入改造，建议方向是：

- 数字人顾客：先做“角色简介页 + 再进入对练”，简介页展示标签、顾客需求、考察重点、适用范围。
- 场景剧本：先做“剧本说明页 + 步骤清单 + 再开始演练”，不要先写死 6 步。
- 金句库：从“Sentence feed”改成“产品金句练习”，先看到产品名、产品图、金句、使用提示，再开始听读/跟读。
- 结果页：首期先收敛，只保留完成、基础得分、建议复习点，不要超前做太复杂的分析维度。

## Practice 模块最近复查结论

### 已经对齐的部分

- `PracticeTaskDetail` 已把任务拆成“数字人顾客 / 场景剧本 / 金句跟读”三类资产，这和后台 `PracticeTaskManage` 的任务组合方式是一致的。
- `AIPractice` 的 `Sentence` 入口已经按“品类 -> 产品线 -> 产品”去展示，方向比之前更接近后台 `BAQuotes`。
- 练习任务详情里已显式展示来源、频次、截止时间、覆盖进度，这部分已经和后台任务管理的表达接近。

### 仍然对不齐的地方

- 数字人顾客页还是偏完整陪练产品，和后台 `BAAvatars` 的“角色配置 / 流程配置”相比，前端的聊天、录音、Finish、结果页仍然做得更重。
- 场景剧本页仍然是固定步数的聊天演练，和后台 `BAScripts` 的“可变步骤 + 每步提示”结构不完全一致。
- 金句练习页虽然已经有产品线和产品层级，但 `SentenceReading` 仍是单句滑动练习，后台更像是产品金句库，不是单纯句子流。
- 练习结果页里的评分维度和反馈文案还是前端自造得偏多，后台并没有对应的配置字段。
- 仍有一些英文型壳文案和产品名暴露在学员端，和后台以业务对象为中心的表达不够统一。

### 结论

- Practice 模块当前是“方向基本对了，但局部表现层还偏重、偏产品化”。
- 后续优先级应是继续把三个练习入口收敛成“先看资产说明，再进入练习”，而不是继续加重结果页和额外交互。
